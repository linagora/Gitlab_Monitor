
# # --- Copyright (c) 2024 Linagora
# # licence       : GPL v3
# # - Flavien Perez fperez@linagora.com
# # - Maïlys Jara mjara@linagora.com

# !! The release token (gitlab access token) used in those jobs will expire on 07/02/2025. !!

.prepare_job:
  rules:
  - if: '$CI_COMMIT_TAG =~ /^v?\d+\.\d+\.\d+$/'
  script:
    - chmod u+x scripts/get-changelogs.sh
    - ./scripts/get-changelogs.sh
    - cat release_notes.md
  artifacts:
    paths:
      - release_notes.md

.release_job:
  rules:
  - if: '$CI_COMMIT_TAG =~ /^v?\d+\.\d+\.\d+$/'
  image: registry.gitlab.com/gitlab-org/release-cli:latest
  script:
    - echo "Creating release"
  release:
    name: 'Release $CI_COMMIT_TAG'
    description: release_notes.md
    tag_name: '$CI_COMMIT_TAG'
    ref: '$CI_COMMIT_SHA'
# TODO: récupérer le code source sur nexus + lien vers le site du projet
    # assets:
    #   links:
    #     - name: 'Container Image $CI_COMMIT_TAG'
    #       url: "https://$CI_REGISTRY_IMAGE/$CI_COMMIT_REF_SLUG:$CI_COMMIT_SHA"